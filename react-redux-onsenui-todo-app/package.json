{
  "name": "src",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "onsenui": "^2.1.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-onsenui": "^1.1.0",
    "react-redux": "^5.0.3",
    "redux": "^3.6.0",
    "redux-thunk": "^2.2.0"
  },
  "devDependencies": {
    "eslint": "^3.16.1",
    "react-scripts": "0.9.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run clean && react-scripts build && cp -r -f ./build/ ./docs/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "clean": "rm -rf ./build/ ; rm -rf ./docs/"
  }
}
